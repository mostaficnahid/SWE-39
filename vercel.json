{
  "buildCommand": "pnpm --filter @workspace/swe39-magazine run build",
  "outputDirectory": "artifacts/swe39-magazine/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
