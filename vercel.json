{
  "buildCommand": "pnpm --filter @workspace/swe39-magazine run build && cp -r artifacts/swe39-magazine/dist dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
